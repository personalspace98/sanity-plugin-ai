import React, { ReactNode } from 'react';
import { DialogTemplateProperties } from '../../types';
type Props = {
    children: ReactNode;
    dialog: DialogTemplateProperties;
};
export declare const DialogDeleteTemplate: (props: Props) => React.ReactNode;
export {};
