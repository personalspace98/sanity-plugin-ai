import React from 'react';
type Props = {
    type?: string;
    tool?: {
        name?: string;
    };
    props: any;
    name?: string;
};
export declare const Index: (props: Props) => React.ReactNode;
export {};
