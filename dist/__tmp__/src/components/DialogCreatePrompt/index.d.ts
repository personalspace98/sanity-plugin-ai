import React, { ReactNode } from 'react';
import { DialogCreatePromptProperties } from '../../types';
type Props = {
    children: ReactNode;
    dialog: DialogCreatePromptProperties;
};
export declare const DialogCreatePrompt: (props: Props) => React.ReactNode;
export {};
