import React, { ReactNode } from 'react';
import { DialogTemplatesProperties } from '../../types';
type Props = {
    children: ReactNode;
    dialog: DialogTemplatesProperties;
};
export declare const DialogTemplates: (props: Props) => React.ReactNode;
export {};
