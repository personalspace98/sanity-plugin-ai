import React, { ReactNode } from 'react';
import { DialogTemplateProperties } from '../../types';
type Props = {
    children: ReactNode;
    dialog: DialogTemplateProperties;
};
export declare const DialogTemplateDelete: (props: Props) => React.ReactNode;
export {};
